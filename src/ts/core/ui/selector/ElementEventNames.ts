// type ElementEvent = (targetElement: IElement, selector: ISelector) => void;

export enum ElementEventNames {
  Over = 'selectorOver',

  Out = 'selectorOut',

  Select = 'select',

  CursorLeft = 'cursorLeft',

  CursorRight = 'cursorRight',

  CursorUp = 'cursorUp',

  CursorDown = 'cursorDown',
}
