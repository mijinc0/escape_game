import * as Locales from '../../core/locales';
import * as events from './events';
import * as items from './items';
import * as credits from './credits';

const ja: Locales.ITextStore = {
  event: events.default,
  item: items.default,
  credits: credits.default,
}

export default ja;